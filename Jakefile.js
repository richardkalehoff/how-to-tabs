( function() {
    'use strict';

    desc( 'Default build' );
    task( 'default', function() {
        console.log( 'Hello, I am the default task.' );

        console.log( '\n\nBUILD OK' );
    } );

} )();
